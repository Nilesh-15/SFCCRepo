<input type="hidden" class="add-to-cart-url" value="${pdict.addToCartUrl}">
<button class="add-to-cart btn btn-primary" data-gtmdata="${JSON.stringify(product.gtmData)}"
    data-gtmga4data="${JSON.stringify(product.gtmGA4Data)}" data-pid="${product.id}" ${!product.readyToOrder || !product.available ? "disabled" : ""}>
    ${Resource.msg('button.addtocart', 'common', null)}
</button>