<iscomment>
    This template is intentionally left blank. It is created to allow a plugin cartridge to render additional buttons, such as for express checkout.
</iscomment>

<isif condition="${pdict.enableBuyNow && customer.authenticated}">
    <isinclude template="product/components/buyNowProduct" />
</isif>
