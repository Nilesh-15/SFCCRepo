<div class="price">
    <span class="starting">
        <iscomment> ${Resource.msg('label.starting_from', 'pricing', null)} </iscomment>
        <isif condition="${product.price.min}">
            ${product.price.min.sales.formatted}
        <iselse>
            ${product.price.sales.formatted}
        </isif>
    </span>
</div>
